{{!-- Render each article with title, link, and Save button --}}
{{!-- <thead>
  <tr>
    <th>
      {{title}}
    </th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>
      {{link}}
    </td>
  </tr>
</tbody> --}}
{{!-- Try Materialize cards --}}
<div class="row">
  <div class="col s12">
    {{!-- <h2 class="header">Horizontal Card</h2> --}}
    <div class="card horizontal change-save" data-id="{{_id}}">
      <div class="card-image">
        <img src="{{image}}">
      </div>
      <div class="card-stacked center-align">
        <div class="card-content">
          <h5>{{title}}</h5>
          <p>{{summary}}</p>
        </div>
        <div class="card-action">
          <a href="{{link}}" class="left">View Article</a>
          <a class="waves-effect waves-light btn cyan lighten-1 right save-article">
            <i class="material-icons right">bookmark</i>Save Article</a>
        </div>
      </div>
    </div>
  </div>
</div>