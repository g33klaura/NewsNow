{{!-- Render each article with title, link, and Save button --}}
{{!-- <thead>
  <tr>
    <th>
      {{title}}
    </th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>
      {{link}}
    </td>
  </tr>
</tbody> --}}
{{!-- Try Materialize cards --}}
<div class="row">
  <div class="col s12 m12">
    {{!-- <h2 class="header">Horizontal Card</h2> --}}
    <div class="card horizontal change-save" id="{{_id}}">
      <div class="card-image">
        <img src="http://via.placeholder.com/100x190">
      </div>
      <div class="card-stacked">
        <div class="card-content valign-center">
          <h5>{{title}}</h5>
        </div>
        <div class="card-action">
          <a href="{{link}}">Visit Article</a>
          <a class="waves-effect waves-light btn">
            <i class="material-icons right">cloud</i>Save Article</a>
        </div>
      </div>
    </div>
  </div>
</div>