{{!-- When "save article" is triggered --}}
<div class="row">
  <div class="col s12">
    {{!-- <h2 class="header">Horizontal Card</h2> --}}
    <div class="card horizontal saved" data-id="{{_id}}">
      <div class="card-image">
        <img src="{{image}}">
      </div>
      <div class="card-stacked center-align">
        <div class="card-content">
          <h5>{{title}}</h5>
        </div>
        <div class="card-action">
          <a href="{{link}}" class="left">View Article</a>
          <a class="waves-effect waves-light btn indigo lighten-3 right article-notes">
            <i class="material-icons right">bookmark</i>Article Notes</a>
          <a class="waves-effect waves-light btn red lighten-3 right unsave-article">
            <i class="material-icons right">delete</i>Delete From Saved</a>
        </div>
      </div>
    </div>
  </div>
</div>